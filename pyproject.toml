[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "arlo"
version = "0.1.0"
description = "ARLO — Automated Remediation Loop Orchestrator (API + Temporal worker)"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
  "claude-agent-sdk",
  "fastapi",
  "temporalio",
  "uvicorn[standard]",
  "asyncpg",
  "psycopg[binary]>=3.1",
  "pgvector>=0.5",
  "mcp>=2.0",
  "httpx",
  "pydantic>=2",
  "pydantic-settings>=2",
  "sqlalchemy>=2",
  "alembic",
  "python-dotenv",
]

[project.optional-dependencies]
dev = [
  "pytest",
  "pytest-asyncio",
  "ruff",
]

[tool.setuptools.packages.find]
include = ["backend*", "worker*"]

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["backend", "worker"]

[tool.ruff]
line-length = 100
target-version = "py311"

[tool.ruff.lint]
# FastAPI idiom: `session: AsyncSession = Depends(get_session)`
# Readiness probes must fail closed on any connectivity error.
ignore = ["B008", "BLE001", "SIM117"]
